const stringLength=(string)=>{
    string ="hello"
    console.log(string.length);
    string.length < 1 || string.length > 10 ? console.log("valid string") : console.log("valid string"); 
}
stringLength();